{
  "name": "remote-browser-control",
  "version": "2.0.0",
  "type": "module",
  "description": "Remote browser control via Cloudflare Workers — no MQTT",
  "main": "rbc-cf.js",
  "scripts": {
    "start": "node rbc-cf.js",
    "dev": "cd cf-workers && npx wrangler dev",
    "deploy": "cd cf-workers && npx wrangler deploy"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
